# Switch-Sound-Samples (optional)

Ohne Dateien synthetisiert die soundEngine die Anschlaege prozedural (src/lib/audio/synth.ts).
Fuer echte Aufnahmen hier ablegen, sie werden automatisch bevorzugt:

- linear.mp3
- tactile.mp3
- clicky.mp3

Kurz (~100-150 ms), mono, ohne Stille am Anfang.
